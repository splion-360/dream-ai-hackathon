[project]
name = "dream-ai-shared-lora-baseline"
version = "0.1.0"
description = "Static shared-LoRA SFT control for Manim code generation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
train = [
    "accelerate>=0.34,<1",
    "bitsandbytes>=0.43,<1",
    "datasets>=2.21,<3",
    "peft>=0.12,<1",
    "safetensors>=0.4,<1",
    "torch>=2.4,<3",
    "transformers>=4.51,<5",
]

[project.scripts]
shared-lora-baseline = "shared_lora_baseline.cli:main"

[dependency-groups]
dev = [
    "mypy>=1.14,<2",
    "pytest>=8,<9",
    "ruff>=0.9,<1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shared_lora_baseline"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true
mypy_path = ["src"]
files = ["src"]
